// 📁 frontend/src/utils/pdfGenerator.js
// Versão simplificada - sem dependências externas
export const pdfGenerator = {
  gerarRelatorioItens: (itens) => {
    console.log('📄 Gerando PDF de itens:', itens);
    // Implementação real virá depois
    return true;
  },

  gerarRelatorioMovimentacoes: (movimentacoes) => {
    console.log('📄 Gerando PDF de movimentações:', movimentacoes);
    return true;
  }
};